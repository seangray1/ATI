<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>JOB_Check_Complex_Loss</fullName>
    <active>true</active>
    <description>Complex Loss cannot be unchecked once it has been checked.</description>
    <errorConditionFormula>AND(
$Profile.Name &lt;&gt; &apos;System Administrator&apos;, 
Complex_Loss__c = FALSE,
ISCHANGED ( Complex_Loss__c ),
OR(
ISPICKVAL(Stage__c, &apos;Won&apos;),
ISPICKVAL(Stage__c, &apos;Lost&apos;),
ISPICKVAL(Stage__c, &apos;Work Complete&apos;),
ISPICKVAL(Stage__c, &apos;Billing Complete&apos;),
NOT(ISBLANK(Last_NCLS_Approval_Date_and_Time__c))
)
)</errorConditionFormula>
    <errorDisplayField>Complex_Loss__c</errorDisplayField>
    <errorMessage>Complex Loss cannot be unchecked if Job has been NCLS Approved, Won or Lost or Completed.</errorMessage>
</ValidationRule>
